fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# LLM gratuito (requer GROQ_API_KEY no .env)
groq>=0.9.0

# Fallback local (baixa ~80MB na primeira execução)
sentence-transformers>=3.0.0
torch>=2.0.0
